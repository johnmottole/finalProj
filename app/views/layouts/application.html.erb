<!DOCTYPE html>
<html>
<head>
  <title>Proj</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>

<% if current_user %>
  <p style="color:blue">Welcome <%= current_user.name %> | <%= link_to "Logout", '/logout' %></p>
<% else %>
  <%= link_to(image_tag("login.png"), '/login') %>
  <%= link_to(image_tag("signup.png"), '/signup') %>

<% end %>

<%= yield %>

</body>
</html>
